import { FormulaireHistoire } from './formulaire-histoire';

describe('FormulaireHistoire', () => {
  it('should create an instance', () => {
    expect(new FormulaireHistoire()).toBeTruthy();
  });
});
